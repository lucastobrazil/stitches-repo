{
  "name": "@maki-ds/icons",
  "version": "0.0.5",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build",
    "test": "echo 📣 test icons"
  },
  "dependencies": {
    "@maki-ds/core": "^0.0.5"
  },
  "devDependencies": {
    "rimraf": "~3.0.2",
    "typescript": "~4.3.0"
  }
}
