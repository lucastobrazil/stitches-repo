{
    "name": "@maki-ds/core",
    "version": "0.0.21",
    "main": "dist/index",
    "types": "dist/index",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "npm run clean && npm run compile",
        "develop": "tsc -p tsconfig.build.json --watch",
        "clean": "rimraf -rf ./dist",
        "compile": "tsc -p tsconfig.build.json",
        "prepublishOnly": "npm run build",
        "test": "echo 📣 core"
    },
    "dependencies": {
        "@genie-solutions/motif-tokens": "^2.1.1",
        "@radix-ui/react-context-menu": "0.0.24",
        "@stitches/react": "^0.2.3",
        "react": "^17.0.0"
    },
    "devDependencies": {
        "rimraf": "^3.0.2",
        "typescript": "~4.3.5"
    }
}
