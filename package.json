{
    "private": true,
    "workspaces": [
        "packages/*",
        "docs"
    ],
    "scripts": {
        "clean": "lerna run clean",
        "build": "lerna run build",
        "pub": "lerna publish",
        "test": "lerna run test --since",
        "diff": "lerna diff"
    },
    "devDependencies": {
        "lerna": "^4.0.0"
    }
}
